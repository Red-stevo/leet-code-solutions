class RomanTointeger
{
    private int toInteger(String roman)
    {
        char romanString[] = new char[roman.length];
        int intArray[] = new int[roman.length];

        romanString = roman.toCharArray();

        for(int i=0;i<roman.length;i++)
        {
            if(romanString[i].)
        }
    }
}